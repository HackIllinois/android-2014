<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hackillinois.android" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="org.hackillinois.android.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="org.hackillinois.android.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_login"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.hackillinois.android.GoogleAuthActivity"
            android:label="@string/title_activity_google_auth" >
        </activity>
        <activity
            android:name="org.hackillinois.android.TempNavActivity"
            android:label="Tempory Navigation Activity" >
        </activity>
        <activity
            android:name="org.hackillinois.android.HelpActivity"
            android:label="@string/title_activity_help" >
        </activity>
        <activity
            android:name="org.hackillinois.android.NewsfeedActivity"
            android:label="@string/title_activity_newsfeed" >
        </activity>
        <activity
            android:name="org.hackillinois.android.ProfileActivity"
            android:label="@string/title_activity_profile" >
        </activity>
        <activity
            android:name="org.hackillinois.android.SkillsActivity"
            android:label="@string/title_activity_skills" >
        </activity>
    </application>

</manifest>
